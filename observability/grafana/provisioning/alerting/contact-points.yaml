apiVersion: 1

# The old mandatory Discord integration is intentionally removed. Configure a
# production contact point only after the team has selected its alert channel.
deleteContactPoints:
  - orgId: 1
    uid: backend-discord
